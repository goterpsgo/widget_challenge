'use strict';

angular
    .module('app')
    .controller('widgets_controller', _controller);

function _controller($scope, __env, WidgetsService) {
    var vm = this;

    _init();

    function _init() {

    }
}

