'use strict';

angular
    .module('app')
    .controller('sizes_controller', _controller);

function _controller($scope, __env, SizesService) {
    var vm = this;

    _init();

    function _init() {

    }
}

